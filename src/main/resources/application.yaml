ktor:
  deployment:
    port: 8080
    shutdown:
      url: "/shutdown"
  application:
    modules:
      - com.slotspace.SerializationKt.configureSerialization
      - com.slotspace.di.NetworkKt.network
      - com.slotspace.RoutingKt.configureRouting
      - com.slotspace.MainKt.mainModule
